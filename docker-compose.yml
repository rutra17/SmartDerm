services:
  # O NOSSO BACK-END
  backend:
    build: ./BackEnd
    ports:
      - "5000:5000" 
    environment:
      - PORT=5000   
    restart: always

  # O NOSSO FRONT-END
  frontend:
    build: ./FrontEnd
    ports:
      - "5173:5173"
    depends_on:
      - backend     
    restart: always
